version: 2
jobs:
  build:
    docker:
      - image: circleci/node:lts
    steps:
      - checkout
      - run:
          name: Install packages
          command: npm install
      - run:
          name: Production build
          command: npm run build